language: java

sudo: required

services:
  - docker


after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    docker build -t echosoft/taxi .
    docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
    docker push echosoft/taxi:latest
    fi